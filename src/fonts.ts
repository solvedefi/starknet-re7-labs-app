import { IBM_Plex_Mono } from 'next/font/google';

export const ibmPlexMonoHeader = IBM_Plex_Mono({
  subsets: ['latin'],
  weight: '700',
});
export const ibmPlexMonoMain = IBM_Plex_Mono({
  subsets: ['latin'],
  weight: '500',
});
export const ibmPlexMonoLight = IBM_Plex_Mono({
  subsets: ['latin'],
  weight: '300',
});
